#include<stdio.h>
#include<math.h>

int main()
{
	char original_log [];
	char new_log [];


//strcat (bir stringi diğerinin sonuna ekler)
//strlen (bir string sayıyısını döndürür)
//strcmp (aynı mı değil mi diye kontrol eder) strcmp ( a,b) == 0 >> doğru
//strlwr (dizeyi küçük harfe çevirir)
//strupr (dizeyi büyük harfe çevirir)
//strrev (dizeyi ters çevirir)

	return 0;
}
